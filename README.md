# AO-Final-Project
## Docker

```sh
docker compose up --build -d
```

## Locally
### Runb Frontend
```sh
cd frontend
npm i
npm run dev
```

### Run Backned
```sh
cd backend
pip install --upgrade pip && pip install --no-cache-dir -r requirements.txt
```